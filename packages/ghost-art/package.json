{
  "name": "@ghostopia/ghost-art",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ghostopia ghost-art — original procedural pixel-art / sprite pipeline (TS-only).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "art:atlas": "tsc -b && node scripts/build_atlas.mjs"
  }
}

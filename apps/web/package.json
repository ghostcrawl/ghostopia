{
  "name": "@ghostopia/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ghostopia web app — React + Vite + PixiJS renderer of server-authoritative state. Thin frontend: renders map DATA + the art atlas, never imports the GhostCrawl SDK or any credential.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@ghostopia/ghost-renderer": "*",
    "@ghostopia/ghost-art": "*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
